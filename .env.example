# Nuxt UI Theme Builder
# This is a fully client-side application — no runtime environment variables are required.

# Context7 MCP API key (used by VS Code MCP server config)
CONTEXT7_API_KEY=

# OpenAI API key for E2E testing of the AI theme generation feature
TEST_OPENAI_API_KEY=
